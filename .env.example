# Local development only. Copy to .env and set your key there.
# On Render, enter the key in the service's environment settings.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
HOST=127.0.0.1
PORT=3000
